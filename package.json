{
  "name": "arrumameucsv",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.cjs",
    "test": "node --test tests/*.test.cjs tests/*.test.mjs",
    "test:browser": "python tests/browser.py",
    "test:accounts": "python tests/accounts-browser.py"
  },
  "devDependencies": {
    "@electric-sql/pglite": "0.5.8"
  }
}
